import { Ubuntu_Mono, VT323 } from 'next/font/google';

export const ubuntuSans = Ubuntu_Mono({
  weight: '400',
  subsets: ['latin'],
});

export const VT323font = VT323({
  weight: '400',
  subsets: ['latin'],
});
